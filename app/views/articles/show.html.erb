<h2><%= @article.title %></h2>
<p><%= @article.body %></p>
<%= link_to "Edit this Article", edit_article_path(@article) %>
<%= link_to "Destroy me!", article_path(@article), data: {
  turbo_method: :delete,
  turbo_confirm: "Are you sure?"
} %>
<%= link_to "All Articles", articles_path %>
