<%= form_with model: [@article, @article.tags.build] do |form| %>
  <p>
    <%= form.label :name, "Tag name" %>
  </p>
  <p>
    <%= form.text_field :name %>
  </p>
  <div>
    <%= form.submit %>
  </div>
<% end %>
